// 6. Escreva uma função que retorne o cubo do valor passado como argumento.

#include <stdio.h>

int cubo(int n) {
  return n * n * n;
}

int main() {
  int num;
  printf("Digite um número inteiro: ");
  scanf("%d", &num);
  printf("O cubo de %d é %d\n", num, cubo(num));
  return 0;
}
